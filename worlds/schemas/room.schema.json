{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://hyperfold.dev/schemas/room.schema.json",
  "title": "Hyperfold Room Schema",
  "description": "Schema for a single sector/room in Hyperfold with 20x20 tile grid and dynamic hazards.",
  "type": "object",
  "required": ["id", "coords", "title", "themeColor", "accentColor", "exits", "grid"],
  "properties": {
    "$schema": {
      "type": "string",
      "description": "Schema reference URI"
    },
    "id": {
      "type": "string",
      "description": "Unique room identifier (e.g. 'room_0_0')",
      "pattern": "^room_[a-zA-Z0-9_-]+$"
    },
    "coords": {
      "description": "Grid coordinate of the room in the hypercube world graph",
      "oneOf": [
        {
          "type": "array",
          "prefixItems": [
            { "type": "integer" },
            { "type": "integer" }
          ],
          "items": false,
          "minItems": 2,
          "maxItems": 2
        },
        {
          "type": "object",
          "required": ["x", "y"],
          "properties": {
            "x": { "type": "integer" },
            "y": { "type": "integer" }
          }
        }
      ]
    },
    "title": {
      "type": "string",
      "description": "Sector title displayed on entry and in HUD (e.g. 'Sector 0: Genesis Core')"
    },
    "subtitle": {
      "type": "string",
      "description": "Optional hint or narrative lore shown on sector entry"
    },
    "themeColor": {
      "type": "string",
      "description": "Primary neon color in hex format (e.g. '#00ffff')"
    },
    "accentColor": {
      "type": "string",
      "description": "Secondary accent color in hex format (e.g. '#0088ff')"
    },
    "exits": {
      "type": "object",
      "description": "Accessible border exits to adjacent hypercube faces. Each direction is either a boolean (true = open exit, false = solid wall) or a gate object for a closed exit requiring a key with matching ID.",
      "required": ["left", "right", "up", "down"],
      "properties": {
        "left": {
          "oneOf": [
            { "type": "boolean", "description": "true = open exit corridor, false = solid wall" },
            {
              "type": "object",
              "description": "Closed exit gated by a key",
              "required": ["id"],
              "additionalProperties": false,
              "properties": {
                "id": { "type": "string", "description": "Unique key ID required to automatically open this exit" },
                "label": { "type": "string", "description": "Optional human-readable label for HUD alerts and tooltips" },
                "color": { "type": "string", "pattern": "^#([A-Fa-f0-9]{6}|[A-Fa-f0-9]{3})$", "description": "Color matching the corresponding gate key" }
              }
            }
          ]
        },
        "right": {
          "oneOf": [
            { "type": "boolean", "description": "true = open exit corridor, false = solid wall" },
            {
              "type": "object",
              "description": "Closed exit gated by a key",
              "required": ["id"],
              "additionalProperties": false,
              "properties": {
                "id": { "type": "string", "description": "Unique key ID required to automatically open this exit" },
                "label": { "type": "string", "description": "Optional human-readable label for HUD alerts and tooltips" },
                "color": { "type": "string", "pattern": "^#([A-Fa-f0-9]{6}|[A-Fa-f0-9]{3})$", "description": "Color matching the corresponding gate key" }
              }
            }
          ]
        },
        "up": {
          "oneOf": [
            { "type": "boolean", "description": "true = open exit corridor, false = solid wall" },
            {
              "type": "object",
              "description": "Closed exit gated by a key",
              "required": ["id"],
              "additionalProperties": false,
              "properties": {
                "id": { "type": "string", "description": "Unique key ID required to automatically open this exit" },
                "label": { "type": "string", "description": "Optional human-readable label for HUD alerts and tooltips" },
                "color": { "type": "string", "pattern": "^#([A-Fa-f0-9]{6}|[A-Fa-f0-9]{3})$", "description": "Color matching the corresponding gate key" }
              }
            }
          ]
        },
        "down": {
          "oneOf": [
            { "type": "boolean", "description": "true = open exit corridor, false = solid wall" },
            {
              "type": "object",
              "description": "Closed exit gated by a key",
              "required": ["id"],
              "additionalProperties": false,
              "properties": {
                "id": { "type": "string", "description": "Unique key ID required to automatically open this exit" },
                "label": { "type": "string", "description": "Optional human-readable label for HUD alerts and tooltips" },
                "color": { "type": "string", "pattern": "^#([A-Fa-f0-9]{6}|[A-Fa-f0-9]{3})$", "description": "Color matching the corresponding gate key" }
              }
            }
          ]
        }
      }
    },
    "spawnPoint": {
      "description": "Player spawn pixel coordinates (x, y). Default: [120, 680]",
      "oneOf": [
        {
          "type": "array",
          "prefixItems": [{ "type": "number" }, { "type": "number" }],
          "items": false,
          "minItems": 2,
          "maxItems": 2
        },
        {
          "type": "object",
          "required": ["x", "y"],
          "properties": {
            "x": { "type": "number" },
            "y": { "type": "number" }
          }
        }
      ]
    },
    "grid": {
      "type": "array",
      "description": "20 rows of 20 characters representing the tile layout. Glyphs: '#' solid, '=' one-way, '^' spike up, 'v' spike down, '<' spike left, '>' spike right, 'B' bounce pad, 'C' crumble block, 'G' goal, '.' or ' ' empty air.",
      "minItems": 20,
      "maxItems": 20,
      "items": {
        "type": "string",
        "minLength": 20,
        "maxLength": 20
      }
    },
    "collectibles": {
      "type": "array",
      "description": "Collectibles placed within the sector (cores, prisms, keys)",
      "items": {
        "type": "object",
        "required": ["id", "type"],
        "properties": {
          "id": { "type": "string" },
          "type": { "type": "string", "enum": ["core", "prism", "key"] },
          "x": { "type": "number" },
          "y": { "type": "number" },
          "pos": {
            "type": "array",
            "prefixItems": [{ "type": "number" }, { "type": "number" }],
            "items": false,
            "minItems": 2,
            "maxItems": 2
          },
          "color": { "type": "string" },
          "label": { "type": "string" }
        }
      }
    },
    "bounceProps": {
      "type": "object",
      "description": "Custom launch velocities for bounce pads, keyed by 'row,col' (e.g. '17,1')",
      "additionalProperties": {
        "type": "object",
        "properties": {
          "vy": { "type": "number", "description": "Vertical velocity (e.g. -1400)" },
          "vx": { "type": "number", "description": "Horizontal velocity (e.g. 200)" }
        }
      }
    },

    "movingPlatforms": {
      "type": "array",
      "description": "Dynamic moving hover platforms",
      "items": {
        "type": "object",
        "required": ["id", "startX", "startY", "endX", "endY", "width", "speed"],
        "properties": {
          "id": { "type": "string" },
          "startX": { "type": "number" },
          "startY": { "type": "number" },
          "endX": { "type": "number" },
          "endY": { "type": "number" },
          "width": { "type": "number" },
          "height": { "type": "number" },
          "speed": { "type": "number" },
          "pauseTime": { "type": "number" },
          "initialProgress": { "type": "number" },
          "themeColor": { "type": "string" },
          "oneWay": { "type": "boolean" }
        }
      }
    },
    "laserBarriers": {
      "type": "array",
      "description": "Timed pulsing or moving laser barriers",
      "items": {
        "type": "object",
        "required": ["id", "startX1", "startY1", "startX2", "startY2"],
        "properties": {
          "id": { "type": "string" },
          "startX1": { "type": "number" },
          "startY1": { "type": "number" },
          "startX2": { "type": "number" },
          "startY2": { "type": "number" },
          "endX1": { "type": "number" },
          "endY1": { "type": "number" },
          "endX2": { "type": "number" },
          "endY2": { "type": "number" },
          "speed": { "type": "number" },
          "pauseTime": { "type": "number" },
          "initialProgress": { "type": "number" },
          "alwaysActive": { "type": "boolean" },
          "activeDuration": { "type": "number" },
          "inactiveDuration": { "type": "number" },
          "warningDuration": { "type": "number" },
          "initialPhase": { "type": "number" },
          "themeColor": { "type": "string" },
          "width": { "type": "number" }
        }
      }
    },
    "laserTurrets": {
      "type": "array",
      "description": "Stationary or timed laser turrets (projectile or continuous beam)",
      "items": {
        "type": "object",
        "required": ["id", "x", "y"],
        "properties": {
          "id": { "type": "string" },
          "x": { "type": "number" },
          "y": { "type": "number" },
          "direction": { "type": "string", "enum": ["left", "right", "up", "down"] },
          "angle": { "type": "number" },
          "autoTarget": { "type": "boolean" },
          "targetRange": { "type": "number" },
          "mode": { "type": "string", "enum": ["projectile", "beam"] },
          "fireInterval": { "type": "number" },
          "fireOffset": { "type": "number" },
          "projectileSpeed": { "type": "number" },
          "projectileLength": { "type": "number" },
          "activeDuration": { "type": "number" },
          "inactiveDuration": { "type": "number" },
          "warningDuration": { "type": "number" },
          "initialPhase": { "type": "number" },
          "themeColor": { "type": "string" }
        }
      }
    },
    "portals": {
      "type": "array",
      "description": "Quantum teleportation portals for intra-sector and inter-sector travel",
      "items": {
        "type": "object",
        "required": ["id", "x", "y"],
        "properties": {
          "id": { "type": "string", "description": "Unique portal identifier" },
          "x": { "type": "number", "description": "Portal X position in pixels" },
          "y": { "type": "number", "description": "Portal Y position in pixels" },
          "width": { "type": "number", "description": "Portal visual width (defaults to 44)" },
          "height": { "type": "number", "description": "Portal visual height (defaults to 68)" },
          "targetPortalId": { "type": "string", "description": "Destination portal unique ID (empty = destination only)" },
          "themeColor": { "type": "string", "description": "Custom neon glow color override" },
          "label": { "type": "string", "description": "Optional human-readable label" },
          "reverseVelocity": { "type": "boolean", "description": "If true, inverts outbound velocity vector (negates vx and vy) when emerging" }
        }
      }
    }
  }
}

